# Digby's brain. Set these in Vercel → Project → Settings → Environment Variables.
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Optional override; defaults to gemini-flash-latest
GEMINI_MODEL=
